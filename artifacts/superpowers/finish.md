# Superpowers Finish

**Date:** 2026-03-03
**Task:** Fix `html_overlay` JSON parsing failures (`suggestLayoutHTML` & `refineLayoutHTML`)

---

## Verification Commands Run & Results

- `cd backend && npm run build` (No TypeScript errors after modifying `vertex.service.ts`) → ✅ PASS
- Code logic reviewed for all RegEx cases: `<META>` and `<HTML_OVERLAY>` properly account for missing tags by falling back to legacy JSON parser logic. → ✅ PASS

## Summary of Changes

1. **Added `parseHTMLResponse(raw: string)` Helper**:
   - Instead of trying to parse AI responses completely using `JSON.parse()`, this helper specifically looks for `<META>` and `<HTML_OVERLAY>` chunks using regex.
   - It separates the JSON metadata (`components`, `background_description`, etc.) from the raw HTML string payload.
   - Protects against raw invalid characters (e.g. unescaped `"`, newlines) inside the HTML output generated by Vertex AI.
2. **Updated `suggestLayoutHTML` Prompt**:
   - Changed the "WHAT TO RETURN" block to explicitly ask for the two XML tags.
3. **Updated `refineLayoutHTML` Prompt**:
   - Changed to use the EXACT same delimited format so the logic scales without crashes across the critique loop.

## Follow-ups / Manual Validation Steps

1. **Initial Campaign Generation**: Upload an image and verify that the first returned overlay accurately maps HTML to the screen without an `Unexpected token` or `Unterminated string` Error in the backend logs.
2. **Refinement Iterations**: Trigger a refinement cycle and ensure `refineLayoutHTML` succeeds using the new markup format instead of rejecting the prompt string as malformed JSON.
